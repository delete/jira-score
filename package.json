{
  "name": "jira-issues",
  "version": "1.3.0",
  "description": "GET a list of issues from Jira scraping the page.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --verbose",
    "testw": "jest --watchAll --verbose --coverage"
  },
  "author": "@delete",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.16.2"
  },
  "devDependencies": {
    "jest": "^20.0.4"
  }
}
