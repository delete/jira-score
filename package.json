{
  "name": "jira-issues",
  "version": "1.3.0",
  "description": "GET a list of issues from Jira scraping the page.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "export ENV_DEV=true && jest --verbose",
    "testw": "export ENV_DEV=true && jest --watchAll --verbose --coverage --runInBand"
  },
  "author": "@delete",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.16.2"
  },
  "devDependencies": {
    "jest": "^20.0.4"
  }
}
