{
  "name": "jira-issues",
  "version": "2.1.0",
  "description": "GET a list of issues from Jira scraping the page.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "export ENV_DEV=true && jest --verbose --coverage",
    "testw": "export ENV_DEV=true && jest --watchAll --verbose --coverage --runInBand"
  },
  "author": "@delete",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.1",
    "nedb": "^1.8.0",
    "@slack/client": "^3.10.0"
  },
  "devDependencies": {
    "jest": "^20.0.4"
  }
}
